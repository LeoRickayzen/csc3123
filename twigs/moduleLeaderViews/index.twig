{% extends 'page.twig' %}

{% block main %}
    <div class = "row">
        <div class = "Jumbotron">
            <div class = "row text-center">
                <h1>Welcome!</h1>
            </div>
            <div class = "row text-center">
                <a href="{{base}}/theme"><button type="button" class="btn btn-primary">View themes and topics</button></a>
            </div>
            <div class = "row text-center">
                <a href="{{base}}/allocate/all"><button type="button" class="btn btn-primary">Allocate students</button></a>
            </div>
            <div class = "row text-center">
                <a href="{{base}}/allocate/themeleader"><button type="button" class="btn btn-primary">Allocate Theme Leaders</button></a>
            </div>
        </div>
    </div>
{% endblock main %}